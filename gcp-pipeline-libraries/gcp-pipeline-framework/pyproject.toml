[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gcp-pipeline-framework"
version = "1.0.6"
description = "Umbrella package for GCP Data Migration Pipeline Libraries"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "gcp-pipeline-core==1.0.5",
    "gcp-pipeline-beam==1.0.5",
    "gcp-pipeline-orchestration==1.0.5",
    "gcp-pipeline-transform==1.0.5",
    "gcp-pipeline-tester==1.0.5",
]

[project.optional-dependencies]
core = ["gcp-pipeline-core==1.0.5"]
beam = ["gcp-pipeline-beam==1.0.5"]
orchestration = ["gcp-pipeline-orchestration==1.0.5"]
transform = ["gcp-pipeline-transform==1.0.5"]
tester = ["gcp-pipeline-tester==1.0.5"]

[tool.setuptools.packages.find]
where = ["src"]
