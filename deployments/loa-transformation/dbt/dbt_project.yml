name: 'loa_transformations'
version: '1.0.0'
config-version: 2

profile: 'loa_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros", "../../gcp-pipeline-gcp-pipeline-libraries/gcp-pipeline-transform/dbt_shared/macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Documentation
docs-paths: ["docs"]

models:
  loa_transformations:
    staging:
      +materialized: view
      +schema: stg_loa
      +tags: ["staging", "loa"]
    fdp:
      +materialized: incremental
      +schema: fdp_loa
      +tags: ["fdp", "loa"]

vars:
  # GCP project
  gcp_project_id: "{{ env_var('GCP_PROJECT_ID') }}"

  # LOA-specific datasets
  source_dataset: "odp_loa"
  staging_dataset: "stg_loa"
  fdp_dataset: "fdp_loa"

  # Extract date filter (optional)
  extract_date: null

# Quoting
quoting:
  database: false
  schema: false
  identifier: false

# Tests
on-run-start: []
on-run-end: []

# Snapshots
snapshots:
  +strategy: timestamp
  +updated_at: _processed_at

